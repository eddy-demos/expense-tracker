# Project

TypeScript monorepo for internal tooling.

## Development

- Use `pnpm` for package management
- Run tests with `pnpm test` before committing
- Follow existing code style, no linting overrides